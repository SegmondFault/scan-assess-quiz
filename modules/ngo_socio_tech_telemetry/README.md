# NGO socio-tech telemetry

This quiz-internal module turns recent scan-assess telemetry into respectful user-context questions for small NGO environments.

It does not scan systems itself. It reads the latest scan-assess output folders and asks whether the respondent recognises devices, websites, services, DNS activity, update ownership, and incident routes.

The output is a JSON question set consumed by the quiz app.
